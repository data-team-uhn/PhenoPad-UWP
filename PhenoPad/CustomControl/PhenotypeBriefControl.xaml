<UserControl
    x:Class="PhenoPad.CustomControl.PhenotypeBriefControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PhenoPad.CustomControl"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:styles="using:PhenoPad.Styles"
    mc:Ignorable="d">

    <UserControl.Resources>
        <Flyout x:Key="PhenotypeDetailFlyout" Placement="Right">
            <local:PhenotypeDetail x:Name="phenotypeDetailControl"/>
        </Flyout>
    </UserControl.Resources>

    <Grid Padding="0 0 0 0" Name="NameGrid" >

        <TextBlock Tapped="nameTextBlockTapped"  Padding="10 0 10 0" FontSize="18" Grid.Column="1" x:Name="phenotypeNameTextBlock" Text="" VerticalAlignment="Center" HorizontalAlignment="Stretch"/>
        <Line Stretch="Fill" Stroke="white" StrokeThickness="2 " X1="1" HorizontalAlignment="Stretch" VerticalAlignment="Center" Name="NameCrossLine" />

    </Grid>
</UserControl>
