<UserControl
    x:Class="PhenoPad.CustomControl.AddInControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PhenoPad.CustomControl"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <UserControl.Resources>
        <x:Double x:Key="categoryButtonHeight">70</x:Double>
        <x:Double x:Key="categoryButtonFontSize">20</x:Double>
    </UserControl.Resources>
    <Grid Background="WhiteSmoke" BorderBrush="LightGray" BorderThickness="2">
        
        <Grid Name="contentGrid">
            <!-- The canvas where we render the replacement text and shapes. -->
            <!--
            <Canvas x:Name="recognitionCanvas" />
            -->
            <!-- The canvas for ink input. -->
            <!--
            <InkCanvas x:Name="inkCanvas" />
            -->
        </Grid>
        <Grid Name="categoryGrid" Background="White">
            <StackPanel Name="SelectionStackPanel"  Width="200"  HorizontalAlignment="Center" VerticalAlignment="Center">
                <Button Style="{StaticResource MyButtonStyle}" HorizontalAlignment="Stretch" Click="DrawingButton_Click"  Name="DrawingButton" Height="{StaticResource categoryButtonHeight}">
                    <TextBlock FontSize="{StaticResource categoryButtonFontSize}" >Drawing</TextBlock>
                </Button>
                <Button Style="{StaticResource MyButtonStyle}" HorizontalAlignment="Stretch" Name="InsertPhotoButton" Click="InsertPhotoButton_Click" Height="{StaticResource categoryButtonHeight}">
                    <TextBlock FontSize="{StaticResource categoryButtonFontSize}" >Insert a photo</TextBlock>
                </Button>
                <Button Style="{StaticResource MyButtonStyle}" HorizontalAlignment="Stretch" Name="TakePhotoButton" Click="TakePhotoButton_Click" Height="{StaticResource categoryButtonHeight}">
                    <TextBlock FontSize="{StaticResource categoryButtonFontSize}" >Take a photo</TextBlock>
                </Button>
            </StackPanel>
        </Grid>
        <StackPanel Height="30" HorizontalAlignment="Right" VerticalAlignment="Top">
            <Button Height="30" Width="30" Padding="0" Name="transformButton" Click="transformButton_Click">
                <SymbolIcon Symbol="ReShare" Height="30" Width="30"></SymbolIcon>
            </Button>
        </StackPanel>
        <StackPanel Height="30" VerticalAlignment="Top">
            <Button Height="30" Width="30" Foreground="Red" Padding="0" Click="Delete_Click">
                <SymbolIcon Symbol="Delete" Height="30" Width="30"></SymbolIcon>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
