<Page
    x:Class="PhenoPad.CustomControl.AddInPreview"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PhenoPad.CustomControl"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">


    <Grid Background="Transparent" Width="200" Height="{x:Bind height}">
        <Border BorderThickness="1" CornerRadius="20">
            
                <Grid Background="WhiteSmoke">
                <InkCanvas x:Name="strokeCanvas" Width="200" Height="auto">
                    <InkCanvas.RenderTransform>
                        <ScaleTransform x:Name="InkCanvasScaleTransform" />
                    </InkCanvas.RenderTransform>
                </InkCanvas>

                <Image x:Name="photo" Width="200" Height="auto" />
                </Grid>

        </Border>
    </Grid>
    
</Page>
